# ch07 查找

本章按“线性空间—二叉搜索树—多路平衡树—散列表”排列。`01_linear_binary.c` 负责边界，`02_bst_avl.c` 和 `03_red_black.c` 负责平衡修复，`04_b_tree.c` 和 `05_b_plus_tree.c` 负责多路结点的查找、插入与分裂，`06_hash.c` 负责冲突和删除标记。

不要背旋转名称来读平衡树。先找新结点到失衡点的路径，再确认哪条有序关系必须保持、哪个高度或颜色约束被破坏。B+ 树内部关键字只负责导航，数据和顺序访问都落在叶结点链；这正是它与 B 树代码最重要的区别。
