# ch06 图

`01_storage.c` 先把同一条边放进邻接矩阵和邻接表；`02_traversal.c` 用 `visited`、栈式递归和队列解释 DFS/BFS；`03_applications.c` 再把遍历扩展为生成树、最短路和有向无环图处理。算法名不同，但共同读法是：当前已经确定了什么，候选状态保存在哪里，哪一步把候选变成确定结果。

尤其注意三个边界：BFS 必须在入队时标记；Dijkstra 的“确定”依赖边权非负；拓扑排序处理不足全部顶点时不是遍历遗漏，而是图中存在有向环。
