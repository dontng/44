# ch04 串

`01_pattern_matching.c` 同时保留 BF 与 KMP。阅读重点不在背 `next` 数组，而在确认两个游标的含义：`i` 指向主串当前字符，`j` 指向模式串当前字符；失配时 BF 改变起点并让主串回退，KMP 只让 `j = next[j]`，主串游标不回退。

本实现采用 0 基下标和 `next[0] = -1`。题目换成 1 基或其他定义时，先从“失配后模式串移动到哪里”重新解释数组，不直接套本文件的数值。
