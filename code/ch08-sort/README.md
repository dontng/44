# ch08 排序

每份源码都围绕一个“已就位区间”来写：插入排序维护有序前缀，冒泡和选择排序维护已归位尾部，快速排序让枢轴归位，堆排序维护大根堆，归并排序维护两个有序子段。`05_compare.c` 用带原始编号的记录验证稳定性，并用快速选择说明“求第 k 小”不必完成全排序。

`06_external_sort.c` 不模拟磁盘接口，只保留 408 真正需要读取的内存机制：败者树让多路归并每次只沿一条根路径调整，最优归并树用较短归并段优先合并来减少总读写量。
