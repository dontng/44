# ch03 栈、队列和数组

先读 `01_stack_queue.c`，只比较每种结构靠什么字段表示状态：栈看 `top`，循环队列看 `front/rear`，双端队列额外用 `size` 区分空满，链队列则看头尾指针。随后读 `02_applications.c`，观察应用并未发明新结构，只是规定了元素何时入栈、出栈、入队和出队。最后用 `03_matrix.c` 训练“二维位置怎样映射到一维下标”，快速转置则训练计数和起始位置预处理。

验收时必须能够判断：`top` 指当前元素还是空位；循环队列是否牺牲一格；右操作数为何先出栈；快速转置为什么不再按每一列重复扫描全部三元组。
